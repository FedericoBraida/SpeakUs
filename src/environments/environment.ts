// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: "AIzaSyBpIgQxQTS5mMNHfIDi_4iEEqH81RQGeBk",
  authDomain: "speak-us.firebaseapp.com",
  databaseURL: "https://speak-us.firebaseio.com",
  projectId: "speak-us",
  storageBucket: "speak-us.appspot.com",
  messagingSenderId: "444276492270",
  appId: "1:444276492270:web:1767e95de332b4350a30ff",
  measurementId: "G-BEHKS9B5ET"
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
